let num = prompt("Enter the integer:  ")
console.log(num);

if (num >= 50 && num <= 100) {
    document.write("integer is in the range of 50-100");
} else {
    document.write("integer is not in the range within 50-100");
}